CSS 무엇인지 개념을 알고 코드에 적용하는 방법 학습하겠습니다.

CSS 가 왜 필요한가?
1. Cascading Style Sheet
2. HTML 의 역할은 정보 표현
3. CSS 의 역할은 HTML 문서를 시각적으로 꾸미는 역할

CSS 적용방법
(1) HTML 문서 <style> 코드 </style>
    웹브라우저(크롬) 한줄 한줄 해석 => 인터프리터 방식
    이 Style 태그 안쪽의 코드는 css 야. css 문법에 맞도록 해석하고 처리해줘

(2)
    외부 CSS 파일 적용

글씨 크기 ,정렬

CSS 선택자(selector)
html에 스타일을 적용할때
html 요소를 지정하는 역활을 한다

내가 작성한 모든 태그에 스타일을 적용하려면
일일이 태그를 적어야 하는가?

-전체 선택자 * 
-유형 선택자 h1 , p , span,
-아이디  선택자 해당 식별된 태그에 대해서만 골라서 적용
-클래스 선택자 : 다중 적용 
-복합 선택자 